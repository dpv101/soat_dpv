<%= form_for(@clase_vehiculo) do |f| %>
  <% if @clase_vehiculo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clase_vehiculo.errors.count, "error") %> prohibited this clase_vehiculo from being saved:</h2>

      <ul>
      <% @clase_vehiculo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
